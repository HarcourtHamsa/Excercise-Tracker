import React, { Component } from 'react'

export default class CreateUser extends Component {
    render() {
        return (
            <React.Fragment>
                Create User page
            </React.Fragment>
        )
    }
}
